# Integrity-check
A C++ console program that takes as argument a path to a directory having MP4 files and uses ffmpeg to check for the integrity of the videos.
FFMPEG should be present in the same directory (folder) as the executable.
